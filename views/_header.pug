header.header
    nav.nav.nav--books
        a.nav__el(href='/') Trending Books
    nav.nav.nav--user
        if user
            a.nav__el.nav__el--logout Log out
            a.nav__el(href='/profile') My Account
            a.nav__el
                img.nav__user-img(src='/img/bookReader.jpg' alt=`Photo of ${user.name}`)
                span=user.name.split(' ')[0]
        else
            a.nav__el(href='/login') Log in
            a.nav__el.nav__el--cta(href='/signup') Sign up
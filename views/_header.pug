header.header
    nav.nav.nav--books
        a.nav__el(href='/') Trending Books
    .book__logo
        img(src='#' alt='Booksreview logo')
    nav.nav.nav--user
        if user
            a.nav__el Log out
            a.nav__el(href='#')
                img.nav__user-img(src='/img/bookReader.jpg' alt='User photo')
                span=user.name.split(' ')[0]
        else
            a.nav__el(href='/login') Log in
            a.nav__el.nav__el--cta(href='/signup') Sign up